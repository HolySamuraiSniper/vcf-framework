---
description: VCF utility — archive loop state from a completed cycle
---

Invoke the `vcf-framework:cleanup` skill to archive in-flight notes, plans, and scratch files once a feature has shipped.
