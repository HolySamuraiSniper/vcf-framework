---
description: VCF utility — install pre-commit hooks for the current repo
---

Invoke the `vcf-framework:setup-precommit` skill to install the recommended pre-commit hooks (typecheck, test, lint) in the current project.
