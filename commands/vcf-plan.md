---
description: VCF gate 4 — produce an executable, vertical-slice plan
---

Invoke the `vcf-framework:plan` skill. Break the PRD into vertical slices (DB → DAL → tool → UI → test), each independently verifiable, with explicit checkpoints between slices.
