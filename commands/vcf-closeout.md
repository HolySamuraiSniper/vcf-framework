---
description: VCF gate 8 — write back learnings, archive, ship
---

Invoke the `vcf-framework:closeout` skill. Commit the work, update memory + ADRs + wiki with what was learned, archive loop state, leave the codebase ready for the next cycle.
