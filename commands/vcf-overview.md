---
description: VCF — alias for /vcf; loads the framework doctrine
---

Invoke the `vcf-framework:overview` skill to load the Vibe Coder Framework doctrine (tiers, gates, anti-patterns, Joey playbook).
