---
description: VCF utility — iteratively refine an existing plan
---

Invoke the `vcf-framework:plan-tune` skill to refine a plan based on feedback, new constraints, or things learned during early build.
