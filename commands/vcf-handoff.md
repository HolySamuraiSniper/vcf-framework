---
description: VCF utility — compact the current session for another agent to pick up
---

Invoke the `vcf-framework:handoff` skill to produce a handoff document so a fresh session (or another agent) can continue without losing state.
